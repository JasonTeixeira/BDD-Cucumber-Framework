[project]
name = "bdd-cucumber-framework"
version = "0.1.0"
description = "Production-grade BDD framework (behave + Playwright) with Allure evidence-first reporting"
requires-python = ">=3.11"

[tool.pytest.ini_options]
addopts = "-q"
